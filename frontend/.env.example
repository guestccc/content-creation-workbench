# 环境变量样例文件
# 使用方法：复制为 .env.local 后按需修改。以 .env.local 结尾的文件已被 .gitignore 忽略。
# 注意：只有 VITE_ 前缀的变量会被暴露给前端代码，切勿在此放置任何密钥。

# 后端接口基础路径
# 开发环境保持相对路径即可，请求会经由 vite.config.ts 中的 proxy 转发到后端
VITE_API_BASE_URL=/api/v1
